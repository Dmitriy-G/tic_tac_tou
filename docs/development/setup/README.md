# Setup

See the root `README.md` for prerequisites and run commands for all three services, and
`docker-compose.yml` to bring up the whole stack with one command.

This directory is reserved for setup notes that don't fit the top-level README (e.g. IDE-specific
configuration, local certificate/proxy quirks) as they come up. Empty for now.
