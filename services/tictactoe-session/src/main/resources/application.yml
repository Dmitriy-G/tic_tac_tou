server:
  port: 8082

spring:
  application:
    name: tictactoe-session

game-engine:
  base-url: ${ENGINE_BASE_URL:http://localhost:8081}

management:
  endpoints:
    web:
      exposure:
        include: health,info