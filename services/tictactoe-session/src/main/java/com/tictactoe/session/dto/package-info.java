/**
 * Request/response DTOs for the session service's REST API. Empty for now — the current
 * endpoints take no request body and still return the domain {@code GameSession} directly.
 */
package com.tictactoe.session.dto;